{
  "name": "edit-release",
  "version": "2.0.0",
  "description": "A GitHub Action to edit GitHub Releases",
  "main": "dist/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "ncc build src/main.js",
    "precommit": "npm run build && git add dist/"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/subnut/edit-release"
  },
  "keywords": [
    "actions",
    "node"
  ],
  "author": "Subhaditya Nath",
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^1.2.6",
    "@actions/github": "^4.0.0",
    "@vercel/ncc": "^0.26.1"
  },
  "devDependencies": {
    "eslint": "^7.16.0"
  }
}
